RESOLVED_DIR="$(
  gz service -s /gazebo/resource_paths/resolve \
    --reqtype gz.msgs.StringMsg \
    --reptype gz.msgs.StringMsg \
    --req 'data: "model://panda_mounted_husky"' \
  | awk -F'"' '/^data:/ {print $2}'
)"

echo "$RESOLVED_DIR"


grep -n "gz_ros2_control" "$RESOLVED_DIR/model.sdf"

